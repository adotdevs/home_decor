export const dynamic = "force-dynamic";
import { connectDb } from "@/lib/db"; import { Article } from "@/models/Article"; import { ArticleCard } from "@/components/article/article-card"; export default async function SubcategoryPage({ params }: { params: Promise<{ categorySlug: string; subcategorySlug: string }> }) { const { categorySlug, subcategorySlug } = await params; await connectDb(); const articles = await Article.find({ categorySlug, subcategorySlug, status: "published" }).sort({ publishedAt: -1 }).limit(24).lean(); return <div className="mx-auto max-w-7xl px-4 py-12 md:px-8"><h1 className="font-heading text-4xl capitalize">{subcategorySlug.replaceAll("-", " ")}</h1><div className="mt-8 grid gap-6 md:grid-cols-3">{articles.map((a:any)=><ArticleCard key={a.slug} article={a} />)}</div></div>; }

