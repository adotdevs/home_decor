export const dynamic = "force-dynamic";
import { connectDb } from "@/lib/db"; import { Article } from "@/models/Article"; import { ArticleCard } from "@/components/article/article-card"; export default async function SearchPage({ searchParams }: { searchParams: Promise<{ q?: string }> }){ const params = await searchParams; const q = params.q || ""; await connectDb(); const articles = q ? await Article.find({ status: "published", $or: [{ title: { $regex: q, $options: "i" } }, { excerpt: { $regex: q, $options: "i" } }] }).limit(30).lean() : []; return <div className="mx-auto max-w-7xl px-4 py-12 md:px-8"><form className="mb-8"><input name="q" defaultValue={q} placeholder="Search decor ideas..." className="w-full rounded-2xl border bg-card px-4 py-3" /></form><div className="grid gap-6 md:grid-cols-3">{articles.map((a:any)=><ArticleCard key={a.slug} article={a} />)}</div></div>; }
