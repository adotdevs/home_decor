import { NextResponse } from "next/server"; import { authCookie } from "@/lib/utils/auth"; export async function POST() { const res = NextResponse.json({ ok: true }); res.cookies.set(authCookie, "", { maxAge: 0, path: "/" }); return res; }
