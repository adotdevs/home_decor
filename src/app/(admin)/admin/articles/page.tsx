export const dynamic = "force-dynamic";
import Link from "next/link"; import { connectDb } from "@/lib/db"; import { Article } from "@/models/Article"; export default async function Page() { await connectDb(); const rows = await Article.find().sort({ updatedAt: -1 }).limit(100).lean(); return <div className="rounded-2xl border bg-card p-6"><div className="mb-4 flex items-center justify-between"><h1 className="font-heading text-3xl">Articles</h1><Link href="/admin/articles/create" className="rounded-xl bg-primary px-4 py-2 text-primary-foreground">Create</Link></div><div className="space-y-2">{rows.map((a:any)=><div key={a.slug} className="flex items-center justify-between rounded-xl border p-3"><div><p className="font-medium">{a.title}</p><p className="text-xs text-muted-foreground">{a.status}</p></div><Link href={`/admin/articles/${a.slug}/edit`} className="text-sm">Edit</Link></div>)}</div></div>; }

