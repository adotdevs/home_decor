import Link from "next/link";
import Image from "next/image";
export function ArticleCard({ article }: { article: { title: string; slug: string; excerpt?: string; categorySlug?: string; readingTime?: number; featuredImage?: string } }) {
  return <article className="group overflow-hidden rounded-3xl border border-black/5 bg-card shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div className="relative h-48 w-full"><Image src={article.featuredImage || `/images/categories/${article.categorySlug || "decoration"}.svg`} alt={`${article.title} inspiration visual`} fill sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw" quality={100} className="object-cover transition duration-300 group-hover:scale-105" /></div><div className="p-5"><p className="text-xs uppercase tracking-[0.18em] text-muted-foreground">{article.categorySlug || "Decor"}</p><h3 className="mt-2 font-heading text-xl leading-snug transition group-hover:text-primary">{article.title}</h3><p className="mt-3 line-clamp-3 text-sm text-muted-foreground">{article.excerpt}</p><div className="mt-4 flex items-center justify-between text-xs text-muted-foreground"><span>{article.readingTime || 5} min read</span><Link href={`/article/${article.slug}`} className="font-semibold text-foreground">Read</Link></div></div></article>;
}