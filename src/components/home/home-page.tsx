import { ArticleCard } from "@/components/article/article-card";
import { AdSlot } from "@/components/ads/ad-slot";
import { FadeIn } from "@/components/common/fade-in";
import { categoryTree } from "@/config/site";
import Image from "next/image";
export function HomePage({ latest }: { latest: any[] }) {
  return <div><FadeIn><section className="mx-auto max-w-7xl px-4 py-14 md:px-8 md:py-20"><p className="text-xs uppercase tracking-[0.2em] text-muted-foreground">Editorial Home Styling</p><h1 className="mt-4 max-w-4xl font-heading text-5xl leading-tight md:text-7xl">Design ideas that make every room feel intentionally beautiful.</h1><p className="mt-5 max-w-2xl text-lg text-muted-foreground">Discover practical, trend-aware decor inspiration crafted like a premium magazine and optimized for real homes.</p></section></FadeIn><section className="mx-auto grid max-w-7xl gap-6 px-4 md:grid-cols-4 md:px-8"><div className="md:col-span-3 grid gap-6 md:grid-cols-2">{latest.slice(0, 4).map((article) => <ArticleCard key={article.slug} article={article as any} />)}</div><AdSlot placement="sidebar" className="h-fit" /></section><section className="mx-auto max-w-7xl px-4 py-14 md:px-8"><h2 className="font-heading text-3xl">Explore by category</h2><div className="mt-6 grid gap-4 sm:grid-cols-2 md:grid-cols-3">{categoryTree.map((category) => <a key={category.slug} href={`/category/${category.slug}`} className="overflow-hidden rounded-3xl border bg-card transition hover:shadow-md"><div className="relative h-40 w-full"><Image src={`/images/categories/${category.slug}.svg`} alt={`${category.name} decor ideas`} fill className="object-cover" /></div><div className="p-6"><h3 className="font-heading text-xl">{category.name}</h3><p className="mt-2 text-sm text-muted-foreground">{category.subcategories.length} focused ideas</p></div></a>)}</div></section><section className="mx-auto max-w-7xl px-4 pb-16 md:px-8"><AdSlot placement="grid" /></section></div>;
}