export const dynamic = "force-dynamic";
import { connectDb } from "@/lib/db"; import { Ad } from "@/models/Ad"; export default async function Page() { await connectDb(); const ads = await Ad.find().sort({ updatedAt: -1 }).lean(); return <div className="rounded-2xl border bg-card p-6"><h1 className="font-heading text-3xl">Ad Management</h1><div className="mt-4 space-y-2">{ads.map((ad:any)=><div key={ad._id.toString()} className="rounded-xl border p-3"><p className="font-medium">{ad.name || ad.placement}</p><p className="text-xs text-muted-foreground">{ad.placement} | {ad.isEnabled ? "Enabled" : "Disabled"}</p></div>)}</div></div>; }

