"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; export default function Page() { const [title, setTitle] = useState(""); const [excerpt, setExcerpt] = useState(""); const [content, setContent] = useState("Start writing..."); const router = useRouter(); async function onSave(status: "draft" | "published") { await fetch("/api/articles", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ title, excerpt, status, categorySlug: "bedroom", subcategorySlug: "bedding-sheets", contentBlocks: [{ type: "paragraph", content }] }) }); router.push("/admin/articles"); } return <div className="rounded-2xl border bg-card p-6"><h1 className="font-heading text-3xl">Create Article</h1><div className="mt-6 space-y-3"><input className="w-full rounded-xl border p-3" placeholder="Title" value={title} onChange={(e)=>setTitle(e.target.value)} /><textarea className="w-full rounded-xl border p-3" rows={3} placeholder="Excerpt" value={excerpt} onChange={(e)=>setExcerpt(e.target.value)} /><textarea className="w-full rounded-xl border p-3" rows={12} placeholder="Content" value={content} onChange={(e)=>setContent(e.target.value)} /><div className="flex gap-3"><button className="rounded-xl border px-4 py-2" onClick={()=>onSave("draft")}>Save Draft</button><button className="rounded-xl bg-primary px-4 py-2 text-primary-foreground" onClick={()=>onSave("published")}>Publish</button></div></div></div>; }
